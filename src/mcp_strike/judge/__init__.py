"""Scoring / LLM-as-judge (placeholder).

Phase 0 stub. The judge module lands in Phase 2 (see docs/PLAN.md §10).
Cost discipline rules: prefer deterministic checks first, default to a cheap
model, and respect the per-run LLM-call cap from configuration.
"""
