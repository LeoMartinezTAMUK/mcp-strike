"""Deliberately-vulnerable demo MCP server.

This package is **intentionally insecure**. It exists to give MCP-Strike a
target during development, tests, and onboarding. Do not deploy it.

Run it with::

    python -m mcp_strike.demo_server

It ships with the installable package (so ``mcp-strike demo`` works after
``pip install mcp-strike``).
"""
